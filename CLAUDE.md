# Claude Configuration

This file configures Claude's behavior and capabilities when working in this repository.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (github.com/aaron-howard/Developer-Workflow-OS). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical labels route work: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: canonical terms in `CONTEXT.md`, architecture decisions in `docs/adr/`. See `docs/agents/domain.md`.
